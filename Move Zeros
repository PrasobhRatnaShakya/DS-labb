void moveZeroes(int* nums, int numsSize) {
    int i,index=0;
    for(i=0;i<numsSize;i++)
    {
        if(nums[i]!=0)
        {
            nums[index]=nums[i];
            index=index+1;

        }
    }
    for (int i=index;i<numsSize;i++)
    {
        nums[i]=0;
    }}
